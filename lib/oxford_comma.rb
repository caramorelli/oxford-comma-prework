def oxford_comma(array)
  case array.length 
  when array.length == 1 
    puts array.first 
  when array.length == 2 
    puts array.first + ' and ' + array.last 
  else 
    list = []
    array.each_with_index do |ele, index|
      
    end 
  end 
end